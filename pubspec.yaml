name: basisuite
description: "Basisuite"
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  freefeos:
    path: ../../FlutterProjects/freefeos
  flutter_mixed:
    path: ../../FlutterProjects/flutter_mixed
  multi_builder:
    path: ../../FlutterProjects/multi_builder

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  module:
    androidX: true
    androidPackage: io.basisuite.flutter
